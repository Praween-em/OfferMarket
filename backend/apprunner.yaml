version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install
      - npx prisma generate
      - npm run build
run:
  command: npm run start:prod
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
